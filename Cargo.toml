[workspace]
resolver = "2"

members = ["macros_process_mining",
	"process_mining", "r4pm",
]

[profile.bench]
opt-level = 3
lto = "thin"
codegen-units = 1
debug = 1

# Full debuginfo on the --all-features test build can take a lot of disk space.
[profile.test]
debug = "line-tables-only"
